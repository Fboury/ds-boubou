{/* DocumentationTemplate.mdx */}

import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ButtonStories from './Button.stories';

<Meta
    of={ButtonStories}
/>

# Button
#### Ceci est un exemple de documentation MDX. Il sert à illustrer quelques possibilités de Storybook via les <a target="_blank" href="https://storybook.js.org/docs/react/writing-docs/doc-blocks">Doc Blocks</a>.

<br />
<br />

## Ceci est un exemple d'insertion directe de code
```js
    import Button from "@maciffr/react-components/components/Button/Button";
```

## Ceci est un exemple de Canvas pour un Button Basic :
<Canvas of={ButtonStories.Basic} sourceState="shown" />
Le bloc code peut être affiché ou caché par défaut et le code copié.

## Ceci est une Story Button Disabled :
<Story of={ButtonStories.Disabled} />
C'est juste un display static d'une Story, mais attend la suite...

## Ceci est un Controls pour un Button Disabled :
<Controls of={ButtonStories.Disabled} />
Il permet d'intéragir dynamiquement avec les args d'une Story ou d'un Canvas. <br />
Par exemple, essaye de cliquer sur False dans la colonne Control. Magique !

<div className="mcf-container">
    <div className="mcf-row">
        <Canvas of={ButtonStories.Disabled} sourceState="shown" className="mcf-col"/>
        <Controls of={ButtonStories.Disabled} className="mcf-col" />
    </div>
</div>
