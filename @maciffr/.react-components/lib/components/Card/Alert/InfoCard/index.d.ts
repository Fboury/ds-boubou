import { ReactNode } from "react";
export interface ContainerProps {
    readonly children: ReactNode;
}
declare function InfoCard({ children }: ContainerProps): import("react/jsx-runtime").JSX.Element;
export default InfoCard;
