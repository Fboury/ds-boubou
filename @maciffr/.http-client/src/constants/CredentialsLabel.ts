export class CredentialsLabel {
    static readonly include: RequestCredentials = "include";
    static readonly omit: RequestCredentials = "omit";
    static readonly sameOrigin: RequestCredentials = "same-origin";
}